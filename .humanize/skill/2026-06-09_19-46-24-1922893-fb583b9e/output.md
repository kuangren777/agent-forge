结论：这个原型不能靠把 `[src/lib/data.ts](/home/lmy/project/2605camel-business/src/lib/data.ts)` 换成几个 `fetch` 就变成真系统，必须把“会话 / 计划 / 审批 / 执行 / 审计 / 探索”都变成后端一等对象；前端的 `[src/lib/appContext.tsx](/home/lmy/project/2605camel-business/src/lib/appContext.tsx)` 只能继续管纯 UI 状态，不能再承载业务真相。

**1. 可更深入的点 + 真实实现清单**
- 当前所有业务真相都堆在 `[src/lib/data.ts](/home/lmy/project/2605camel-business/src/lib/data.ts)` 和 `[src/lib/appContext.tsx](/home/lmy/project/2605camel-business/src/lib/appContext.tsx)`；建议新增 `server/`，选 `Python 3.12 + FastAPI + SQLAlchemy 2 + Alembic + PostgreSQL + Redis + SSE`。目录先落成 `server/app/api`, `server/app/models`, `server/app/services`, `server/app/policies`, `server/app/executors`, `server/app/plugins`, `server/app/realtime`, `server/app/workers`，前端保留在现仓库根目录。
- 先拆“UI 常量”和“业务数据”；`[src/lib/data.ts](/home/lmy/project/2605camel-business/src/lib/data.ts)` 里的 `NAV` 可迁到 `src/config/navigation.ts`，其余 `DATA_SOURCES / OPS / AUDIT_EVENTS / FLOW_NODES / CHAT / PLUGINS / LIVE_STREAM` 全部删除，改为 `src/api/*` 查询。
- 前端状态层要分层；`[src/lib/appContext.tsx](/home/lmy/project/2605camel-business/src/lib/appContext.tsx)` 只保留 `active/treeSel/query/toast` 这类壳状态，新增 `src/api/http.ts`, `src/api/queryClient.ts`, `src/api/types.ts`, `src/features/*/queries.ts`，并引入 `@tanstack/react-query` 管远端缓存、重试、失效和并发请求。
- 建议把后端领域拆成 6 个 bounded contexts：`identity`、`sources`、`registry`、`chat/plans`、`executions/approvals`、`audit/traces`；这样 7 个 screen 都能映射到稳定后端对象，而不是继续按页面拼接口。
- 数据库第一层表建议这样建：`users`, `roles`, `user_roles`, `sessions`, `tenants`；每张业务表都带 `tenant_id`。否则以后做企业多租户会返工。
- 数据源与探索建议建表：`data_sources(id, tenant_id, type, name, connector_kind, status, config_json, secret_ref, created_by, created_at)`, `exploration_jobs(id, source_id, trigger_type, status, phase, progress, started_by, started_at, finished_at)`, `exploration_events(id, job_id, seq, event_type, payload_json, created_at)`；Explore/Live 两屏都围绕 `source_id` 和 `job_id` 跑。
- 探索产物不要直接塞成 UI 文案；建议建 `discovered_entities`, `discovered_operations`, `discovered_rules`, `discovered_chains`。`discovered_operations` 至少要有 `key`, `kind`, `input_schema`, `output_schema`, `capability_requirements`, `executor_hint`, `source_job_id`。
- Operation Registry 要版本化；建 `operations(id, op_key, version, kind, confirm_level, risk_level, status, input_schema_json, output_schema_json, executor_binding, rollback_binding, created_from_job_id, published_at)`。不要再用当前 `[src/lib/types.ts](/home/lmy/project/2605camel-business/src/lib/types.ts)` 里那种只有 `name/type/perm/confirm/status/roles` 的薄模型。
- 权限不要写成字符串 `perm`；单独建 `operation_permissions(op_version_id, subject_type, subject_id, effect, condition_json)`，支持 role-based 和 attribute-based 条件，例如“只能查本人”“只能查本部门”“必须二审”。
- 审批流建议建 `approval_requests(id, trace_id, target_type, target_id, confirm_level, status, requested_by, expires_at)` 和 `approval_votes(id, request_id, approver_id, decision, comment, created_at)`，加唯一索引 `(request_id, approver_id)`；这样 `confirm` 和 `dual_approval` 才是完整流程，不是改一个状态位。
- Chat 真实化要把“会话、消息、计划、步骤、确认”拆开；建 `chat_sessions`, `chat_messages`, `execution_plans`, `plan_steps`。`plan_steps` 至少要存 `step_no`, `kind(query/parse/write)`, `op_key`, `input_refs`, `capability_in`, `capability_out`, `approval_request_id`, `status`。
- API 端点建议最少包括：`GET /api/v1/me`, `POST /api/v1/auth/logout`, `GET/POST/PATCH /api/v1/sources`, `POST /api/v1/sources/:id/explore`, `GET /api/v1/exploration-jobs/:id`, `GET /api/v1/exploration-jobs/:id/events`, `GET /api/v1/operations`, `GET /api/v1/operations/:id`, `POST /api/v1/operations/:id/publish`, `POST /api/v1/approval-requests/:id/votes`, `POST /api/v1/chat/sessions`, `POST /api/v1/chat/sessions/:id/messages`, `POST /api/v1/plans/:id/confirm`, `POST /api/v1/plans/:id/cancel`, `GET /api/v1/traces/:id/flow`, `GET /api/v1/traces/:id/audit`, `POST /api/v1/executions/:id/rollback`, `GET /api/v1/plugins`, `POST /api/v1/plugins/registrations`。
- P-LLM 不要输出“展示代码字符串”，要输出严格 schema；新增 `server/app/services/planner.py`，让 P-LLM 只产出 `PlanDraft` JSON，字段包括 `intent`, `steps`, `writes`, `required_confirm_level`, `reasoning_summary`, `policy_hints`。前端 `[src/screens/Chat.tsx](/home/lmy/project/2605camel-business/src/screens/Chat.tsx)` 展示的是后端返回的结构化计划，不是本地脚本。
- Q-LLM 要真隔离；新增 `server/app/services/qparser.py` 和 `server/app/sandbox/`，Q-LLM 只能接收受限数据切片或对象引用，输出类型化选择结果、分类结果或抽取结果，绝不直接拿执行权限，也绝不把输出标成 `trusted`。
- P-LLM / Q-LLM 调用本身也要入库；建 `llm_runs(id, trace_id, llm_role, provider, model, prompt_version, input_ref, output_json, token_usage, latency_ms, safety_flags)`。这样 Audit 才能回答“这个计划是哪次模型调用产生的”。
- 策略引擎建议先落 Python DSL，再抽 OPA；文件可以是 `server/app/policies/base.py`, `server/app/policies/registry.py`, `server/app/policies/rules/refund.py`。执行顺序是“请求入站约束注入 → 计划审核 → 步骤级 allow/deny → 执行后校验”，不是只在最后判断一次。
- 执行器要真正可插拔；做 `ExecutorRegistry`，实现 `api_executor.py`, `function_executor.py`, `sql_executor.py`, `rpa_executor.py`。`operations.executor_binding` 指向具体实现，执行时统一落 `executions` 表，字段包括 `before_state`, `after_state`, `idempotency_key`, `latency_ms`, `error_code`。
- 回滚不要是 toast；真实做法是 `rollback_binding` + `compensation plan`。`POST /api/v1/executions/:id/rollback` 触发补偿执行，并把补偿 trace 与原 trace 关联。
- 审计链建议用 append-only 表 `audit_events(id, trace_id, seq, event_type, actor_id, payload_json, prev_hash, hash, created_at)`；写入时对规范化 JSON 做哈希，链头用事务锁定，另外定期把 `trace root hash` 或日分区 Merkle root 锚定到对象存储或外部不可变介质。
- Flow 图不要另造一份数据；建 `dataflow_nodes(trace_id, node_id, label, capability_set, source_kind, produced_by_step_id)` 和 `dataflow_edges(trace_id, from_node_id, to_node_id, transform_kind)`，由执行计划和 Q-LLM 结果自动派生，Flow/Audit 共享同一 `trace_id`。
- 实时通道优先用 SSE，不必一上来就 WebSocket；`GET /api/v1/exploration-jobs/:id/events` 给 Live，`GET /api/v1/traces/:id/events` 给 Chat/Audit/Flow。事件必须带 `seq` 和 `Last-Event-ID` 恢复能力。
- 前端 7 个 screen 的改造点很明确：`[src/screens/Explore.tsx](/home/lmy/project/2605camel-business/src/screens/Explore.tsx)` 改为 `useSources/useStartExplore`；`[src/screens/Live.tsx](/home/lmy/project/2605camel-business/src/screens/Live.tsx)` 改为 `useExplorationJob/useExplorationStream`；`[src/screens/Chat.tsx](/home/lmy/project/2605camel-business/src/screens/Chat.tsx)` 改为 `useChatSession/useSendMessage/useConfirmPlan`；`[src/screens/Ops.tsx](/home/lmy/project/2605camel-business/src/screens/Ops.tsx)` 改为 `useOperations/useApproveRequest`；`[src/screens/Flow.tsx](/home/lmy/project/2605camel-business/src/screens/Flow.tsx)` 改为 `useTraceFlow`；`[src/screens/Audit.tsx](/home/lmy/project/2605camel-business/src/screens/Audit.tsx)` 改为 `useTraceAudit/useRollbackExecution`；`[src/screens/Plugins.tsx](/home/lmy/project/2605camel-business/src/screens/Plugins.tsx)` 改为 `usePlugins/useRegisterPlugin`。
- 前端获取/缓存/loading/error 的标准做法：列表页 `staleTime` 30-60s；高风险 mutation 不做“成功乐观更新”，只做“按钮 pending”；聊天发送可以乐观插入本地消息，但计划确认和执行结果必须等服务端 ack；每屏都加 skeleton、空态、失败重试、权限失败态。
- `[src/screens/registry.tsx](/home/lmy/project/2605camel-business/src/screens/registry.tsx)` 里的标题、副标题、数量和 trace 文案现在都是写死的；真实化后只保留页面元信息，动态数字全部来自接口返回，例如 `pendingApprovalCount`, `currentTraceId`, `jobProgress`。
- 认证建议直接接企业 IdP，优先 `OIDC/SAML + HttpOnly session cookie`；`[src/components/Shell.tsx](/home/lmy/project/2605camel-business/src/components/Shell.tsx)` 的 role switch 不再代表权限，只能保留成 admin 专用的“impersonation/debug view”，并强制写审计。
- RBAC 要后端强制；`GET /me` 返回 `roles`, `scopes`, `allowed_screens`, `feature_flags`，前端只据此渲染，真正的数据过滤和写操作授权全部在 API 层做。
- 性能上，Audit/Live/Chat 日志都要分页或虚拟滚动；给 `audit_events(trace_id, seq)`, `exploration_events(job_id, seq)`, `chat_messages(session_id, created_at)`, `approval_requests(status, created_at desc)` 建索引；大 prompt、before/after snapshot 放对象存储，DB 里存引用和摘要。
- 测试也要从“组件演示测试”切成“契约 + 集成”；当前 `[src/screens/Chat.test.tsx](/home/lmy/project/2605camel-business/src/screens/Chat.test.tsx)`、`[src/screens/Ops.test.tsx](/home/lmy/project/2605camel-business/src/screens/Ops.test.tsx)` 主要在验证 mock 交互，真实化后应补 `policy` 单测、`approval` 流程测试、`audit hash` 一致性测试、前后端契约测试、Playwright 端到端。

**2. 逻辑上有问题的地方**
- 权限现在只是前端过滤；`[src/lib/data.ts](/home/lmy/project/2605camel-business/src/lib/data.ts)` 的 `ROLE_NAV` 和 `[src/lib/appContext.tsx:69](/home/lmy/project/2605camel-business/src/lib/appContext.tsx#L69)` 只是在本地切 screen，任何真实数据一旦也按这个模式下发到浏览器，就是“未授权数据已下发，只是没显示”。要改成后端按身份返回数据，前端不拿无权数据。
- `role` 切换是假身份系统；`[src/lib/appContext.tsx:52](/home/lmy/project/2605camel-business/src/lib/appContext.tsx#L52)` 直接 `useState('admin')`，这会误导设计。真实系统必须区分“认证身份”“授权角色”“调试模拟身份”，三者都要审计。
- Operation 审批完全不持久；`[src/lib/appContext.tsx:77](/home/lmy/project/2605camel-business/src/lib/appContext.tsx#L77)` 和 `[src/lib/appContext.tsx:81](/home/lmy/project/2605camel-business/src/lib/appContext.tsx#L81)` 只是本地改 `status`。这意味着刷新丢失、多人看不到同一状态、无法回放“谁在何时批准了什么”。
- `dual_approval` 流程不完整；`[src/screens/Ops.tsx:96](/home/lmy/project/2605camel-business/src/screens/Ops.tsx#L96)` 显示“双人确认”，但 `[src/screens/Ops.tsx:119](/home/lmy/project/2605camel-business/src/screens/Ops.tsx#L119)` 直接把状态设为 `active`。没有唯一 approver 约束、没有第二人校验、没有超时、没有拒绝后状态机。
- `approveAllPending` 逻辑直接绕过确认等级；`[src/components/Shell.tsx:36](/home/lmy/project/2605camel-business/src/components/Shell.tsx#L36)` 批量批准所有 pending，和 `confirm/dual` 的安全模型本身冲突。真实系统里批量动作最多只能“发起审批”，不能批量越过人审。
- 类型系统里已经有不自洽；`[src/lib/types.ts:16](/home/lmy/project/2605camel-business/src/lib/types.ts#L16)` 的 `OpsStatus` 只有 `active | pending`，但 `[src/screens/Ops.tsx:11](/home/lmy/project/2605camel-business/src/screens/Ops.tsx#L11)` 又硬 cast 一个 `disabled`。这说明 UI、类型、状态机三者已经分叉，真实化前必须先统一枚举。
- `OpsConfirm` 枚举和值文案不统一；`[src/lib/types.ts:15](/home/lmy/project/2605camel-business/src/lib/types.ts#L15)` 用 `dual`，UI 文案却到处写 `dual_approval`。真实 API 枚举必须一次定死，否则前后端很容易出现兼容 bug。
- `Operation` 同时有 `perm` 和 `roles` 两套权限表示；`[src/lib/types.ts:19](/home/lmy/project/2605camel-business/src/lib/types.ts#L19)` 和 `[src/lib/data.ts:200](/home/lmy/project/2605camel-business/src/lib/data.ts#L200)` 里这两套信息是冗余且易冲突的。真实系统应只保留“策略引用/权限规则”，不要在 UI model 里再重复一套角色数组。
- Chat 的“确认执行”是假成功；`[src/screens/Chat.tsx:94](/home/lmy/project/2605camel-business/src/screens/Chat.tsx#L94)` 先 `setDone(true)`，再 toast“已执行”。这会把“用户确认成功”“执行成功”“审计落链成功”三个不同阶段混成一个布尔值。
- Chat 没有会话 id / plan id / trace id；`[src/screens/Chat.tsx](/home/lmy/project/2605camel-business/src/screens/Chat.tsx)` 里的对话、计划、执行结果都只是当前组件内存。真实系统里任何一步失败、重试、回放、导出都无从谈起。
- P-LLM / Q-LLM 边界只是文案，不是隔离；`[src/lib/data.ts:111](/home/lmy/project/2605camel-business/src/lib/data.ts#L111)` 的 `PLLLM_CODE` 和 `[src/lib/data.ts:179](/home/lmy/project/2605camel-business/src/lib/data.ts#L179)` 的 `FLOW_NODES` 都是展示文本，没有任何“哪些数据进入了 Q-LLM、哪些参数被策略改写、哪些步骤可执行”的真实证据链。
- “Q-LLM 输出继承输入能力”这句话过于粗；`[src/screens/Flow.tsx:76](/home/lmy/project/2605camel-business/src/screens/Flow.tsx#L76)` 用单一 `cap` 表示能力，但现实里能力是格 lattice，不是单值枚举。多输入合并、trusted 与 data 混合、parsed 再经人工确认后的升级规则，目前都没定义。
- `CapKind` 设计过窄；`[src/lib/types.ts:7](/home/lmy/project/2605camel-business/src/lib/types.ts#L7)` 只有一个 `cap`，这会逼你把复杂 provenance 压扁成单标签。真实实现至少要支持 `capability_set`, `provenance_refs`, `transform_kind`, `approved_by`。
- 审计链是写死文本，不是链；`[src/screens/Audit.tsx:24](/home/lmy/project/2605camel-business/src/screens/Audit.tsx#L24)` 每条都显示同样的 `hash 9f3a… ← prev e21c…`。没有事件序号、没有规范化 payload、没有链头锁、没有验证入口，所以“完整性已验证”只是 UI 叙事。
- Flow 与 Audit 与 Chat 没有共享 trace 主键；`[src/screens/registry.tsx:45](/home/lmy/project/2605camel-business/src/screens/registry.tsx#L45)`、`[src/screens/registry.tsx:67](/home/lmy/project/2605camel-business/src/screens/registry.tsx#L67)`、`[src/lib/data.ts:210](/home/lmy/project/2605camel-business/src/lib/data.ts#L210)` 都在写 `abc123`，但三者不是从同一数据源派生，逻辑上无法证明“这个计划对应这个执行对应这条审计链”。
- Live 页面不是实时，是定时器动画；`[src/screens/Live.tsx:31](/home/lmy/project/2605camel-business/src/screens/Live.tsx#L31)` 只是 `setInterval` 截取本地数组，且到末尾后重置到 4。真实日志流必须有 `job_id + seq + reconnect + done state`。
- Plugins 只是展示“接口稳定”，没有真正的扩展契约；`[src/screens/Plugins.tsx](/home/lmy/project/2605camel-business/src/screens/Plugins.tsx)` 和 `[src/lib/data.ts:221](/home/lmy/project/2605camel-business/src/lib/data.ts#L221)` 没有版本、签名、兼容矩阵、权限声明、健康检查、隔离级别。真接第三方实现时这是明显安全洞。
- 现在“回滚”也只是 toast；`[src/screens/Audit.tsx:61](/home/lmy/project/2605camel-business/src/screens/Audit.tsx#L61)` 没有补偿策略、没有回滚前状态快照、没有幂等约束。很多外部副作用根本不能直接 reverse，必须设计 compensation。
- `[src/screens/registry.tsx](/home/lmy/project/2605camel-business/src/screens/registry.tsx)` 把 “8 个写操作待审核”“73%”“5 节点 4 边” 这类动态数写死，逻辑上迟早与真实后端脱节；这些应由 API 聚合返回，不该存在于静态 screen config。
- `[src/App.tsx](/home/lmy/project/2605camel-business/src/App.tsx)` 没有路由，所有对象选择都靠内存索引；真实系统里 `trace/session/op/job` 必须可深链、可刷新恢复、可分享，否则审计和审批闭环做不起来。
- 现有测试覆盖的是“本地 demo 行为”，不是“系统正确性”；例如 role-based row filtering 本质上在验证前端权限过滤，这在真实安全系统里不是正确目标。测试基线本身需要重设。

如果你要继续推进到“真能跑”的方案，下一步最值钱的是先做一版最小真实骨架：`Auth + Sources + ExplorationJob(SSE) + Operations + ApprovalRequest + ChatPlan + AuditEvent(hash)`，前端先把 `[src/lib/data.ts](/home/lmy/project/2605camel-business/src/lib/data.ts)` 清空到只剩 UI 常量，再逐屏切 API。这样不是“把 mock 改复杂”，而是开始有真正系统边界。
